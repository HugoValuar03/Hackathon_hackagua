name: hackathon_hackagua
description: A new Flutter project.

publish_to: 'none' # Remove se for publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  flutter_map: ^6.0.1
  latlong2: ^0.9.0
  geolocator: ^14.0.2
  flutter_svg: ^2.0.9
  lottie: ^2.7.0
  fl_chart: ^0.66.1
  intl: ^0.19.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
